import { DatefromstartPipe } from './datefromstart.pipe';

describe('DatefromstartPipe', () => {
  it('create an instance', () => {
    const pipe = new DatefromstartPipe();
    expect(pipe).toBeTruthy();
  });
});
